const foundation = (slug, stateful = false, driver = null) => ({ fixture: 'foundation', selector: `[data-slug="${slug}"]`, stateful, driver });
const overlays = (slug, stateful = true, driver = 'overlays') => ({ fixture: 'overlays', selector: `[data-slug="${slug}"]`, stateful, driver });
const advanced = (slug, stateful = true, driver = 'advanced') => ({ fixture: 'advanced', selector: `[data-slug="${slug}"]`, stateful, driver });

// Keep this list explicit: additions to the source registry must choose an SSR and interaction policy.
export const completeFixtures = {
	button: foundation('button', true, 'foundation'),
	input: foundation('input', true, 'foundation'),
	badge: foundation('badge'),
	separator: foundation('separator'),
	toggle: foundation('toggle', true, 'foundation'),
	card: foundation('card'),
	dialog: foundation('dialog', true, 'foundation'),
	alert: foundation('alert'),
	'aspect-ratio': foundation('aspect-ratio'),
	attachment: foundation('attachment', true, 'foundation'),
	avatar: foundation('avatar', true, 'foundation'),
	breadcrumb: foundation('breadcrumb'),
	bubble: foundation('bubble'),
	empty: foundation('empty'),
	item: foundation('item'),
	kbd: foundation('kbd'),
	label: foundation('label'),
	marker: foundation('marker'),
	skeleton: foundation('skeleton'),
	spinner: foundation('spinner'),
	table: foundation('table'),
	'button-group': foundation('button-group'),
	checkbox: foundation('checkbox', true, 'foundation'),
	field: foundation('field'),
	form: foundation('form', true, 'foundation'),
	'input-group': foundation('input-group', true, 'foundation'),
	'input-otp': foundation('input-otp', true, 'foundation'),
	'native-select': foundation('native-select', true, 'foundation'),
	progress: foundation('progress'),
	'radio-group': foundation('radio-group', true, 'foundation'),
	slider: foundation('slider', true, 'foundation'),
	switch: foundation('switch', true, 'foundation'),
	textarea: foundation('textarea', true, 'foundation'),
	'toggle-group': foundation('toggle-group', true, 'foundation'),
	accordion: foundation('accordion', true, 'foundation'),
	calendar: foundation('calendar', true, 'foundation'),
	collapsible: foundation('collapsible', true, 'foundation'),
	direction: foundation('direction'),
	message: foundation('message'),
	'message-scroller': foundation('message-scroller'),
	pagination: foundation('pagination'),
	'scroll-area': foundation('scroll-area'),
	tabs: foundation('tabs', true, 'foundation'),
	'alert-dialog': overlays('alert-dialog'),
	combobox: overlays('combobox'),
	command: overlays('command'),
	'context-menu': overlays('context-menu'),
	drawer: overlays('drawer'),
	'dropdown-menu': overlays('dropdown-menu'),
	'hover-card': overlays('hover-card'),
	menubar: overlays('menubar'),
	'navigation-menu': overlays('navigation-menu'),
	popover: overlays('popover'),
	select: overlays('select'),
	sheet: overlays('sheet'),
	tooltip: overlays('tooltip'),
	carousel: advanced('carousel'),
	chart: advanced('chart', false, null),
	resizable: advanced('resizable'),
	sidebar: advanced('sidebar'),
	sonner: advanced('sonner'),
};

export const strictIdentitySelectors = {
	foundation: ['[data-stage5-root="foundation"]', '[data-control="input"]', '[data-control="checkbox"]', '[data-control="accordion-trigger"]', '[data-control="tabs-package"]'],
	overlays: ['[data-stage5-root="overlays"]', '[data-control="select-trigger"]', '[data-control="combobox"]', '[data-control="popover-trigger"]'],
	advanced: ['[data-stage5-root="advanced"]', '[data-control="carousel"]', '[data-control="resizable"]', '[data-control="sidebar-trigger"]', '[data-control="toaster"]'],
};
